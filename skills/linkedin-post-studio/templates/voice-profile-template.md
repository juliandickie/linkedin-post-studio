---
# ============================================================================
# VOICE PROFILE TEMPLATE — handoff produced by onboarding/voice-capture.md
# Copy the `voice:` block below into the profile's main file under the `voice:`
# frontmatter key. The "Voice observations from capture" markdown body goes
# into the profile's body under its own heading.
# ============================================================================
---

voice:
  status: complete                    # REQUIRED — missing | partial | complete
  archetype: Operator                 # RECOMMENDED — Visionary | Operator | Processor
  adjectives: [direct, evidence-based, practical, dry-witted]   # 3-5 adjectives
  emoji_policy: minimal               # none | minimal | moderate
  sentence_rhythm: medium-balanced    # short-staccato | medium-balanced | long-flowing
  banned_phrases:
    - delve
    - dive deep
    - unlock
    - leverage                        # as verb
    - synergy
    - journey
    - tapestry
    - testament
    - ever-evolving
    - resonate
    # Add user's own below:
    - <user-added phrase>
  signature_phrases:
    - "<phrase the user reuses, e.g., 'the math doesn't lie'>"
  voice_samples_path: ~/Documents/LinkedIn Post Studio/voice-samples/<profile_id>/
  voice_samples_inline: 0             # count of inline samples in profile body below

# ============================================================================
# Markdown body — append to profile's body under this heading:
# ============================================================================

## Voice observations from capture

- **Opening patterns:** <e.g., "Always opens with a specific number or proper noun. 7 of 10 samples started with a figure.">
- **Typical sentence length:** <e.g., "8–14 words, rarely over 20. Uses 3-word sentences for punctuation.">
- **Signature moves:** <e.g., "Uses dashes for asides. Contrasts 'old' vs 'new' approaches explicitly.">
- **Closing structures:** <e.g., "Ends with a single-line observation, not a question. Avoids 'Thoughts?' entirely.">
- **Formatting habits:** <e.g., "Minimal emoji use, none in hook. Line breaks between every paragraph. Occasional all-caps for emphasis, max once per post.">
- **Detected archetype:** <Visionary | Operator | Processor> — <brief rationale pointing to samples>
